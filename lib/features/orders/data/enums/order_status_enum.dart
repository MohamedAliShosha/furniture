// contains order status like confirmed, processing, shipped, outOfDelivery, delivered, cancelled
enum OrderStatusEnum {
  confirmed,
  processing,
  shipped,
  outForDelivery,
  delivered,
  cancelled,
}
